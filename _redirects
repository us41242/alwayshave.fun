# Cloudflare Pages routing rules
# These rewrite /{state}/{slug} to trail.html without changing the URL (200 = rewrite)

/:state/:slug   /site/trail.html   200
/:state         /site/index.html   200
/               /site/index.html   200
